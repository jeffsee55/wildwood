/**
 * Same-origin `localStorage` key for the active git ref in the embedded editor.
 * Must match {@link TR33_ACTIVE_REF_STORAGE_KEY} in `packages/extension/src/host-bridge.ts`.
 */
export const TR33_ACTIVE_REF_STORAGE_KEY = "tr33.activeRef";
